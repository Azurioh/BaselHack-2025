/**
 * @enum ResponseType
 * @description Enum for the response type
 */
export enum ResponseType {
  TEXT = 'text',
  NUMBER = 'number',
  BOOLEAN = 'boolean',
  CHOICE = 'choice',
  MUTLIPLE_CHOICE = 'multiple-choice',
  OPEN = 'open',
}
